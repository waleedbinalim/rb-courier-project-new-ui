

export default function useDeleteHook() {
    // Need two things to be passed down here as initial state, the first being
    // Whether it's an order to be deleted or a user
    // Then we also need the ID, also need to make this an async function
    // Also need the users and diltered users state
}
